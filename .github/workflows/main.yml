name: Licensing

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}

on:
  workflow_dispatch:
  pull_request:
    paths-ignore:
      - 'README.md'

jobs:
    license_finder:
        uses: viamrobotics/mti/.github/workflows/license_finder.yml@main
